# TURN server config
listening-port=3478
fingerprint
lt-cred-mech
user=webrtc:webrtc
realm=rtsp-webrtc
# External IP (replace with your actual IP if needed)
external-ip=127.0.0.1
# Allow localhost connections
no-stun
# Logging
verbose
log-file=/tmp/turnserver.log
