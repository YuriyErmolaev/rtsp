services:

  # Angular-based webapp over angie (multistage pipeline)
  webapp:
    image: ${DOCKER_REGISTRY}/${APP_ENV:-app}_webapp:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - ${WEBAPP_HTTP_PORT:-5300}:80
    environment:
      TZ: "Europe/Moscow"
    networks:
      - ${APP_ENV:-app}-net
